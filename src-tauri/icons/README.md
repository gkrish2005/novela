# App Icon

Drop `icon.icns` here (1024x1024 PNG converted via `iconutil` or Image2icon).
`tauri.conf.json` already points at `icons/icon.icns`.

Until you have real artwork, Tauri's default icon will be used — the app will
still build and run fine without this.
