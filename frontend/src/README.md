# frontend/src

Empty on purpose — scaffold the actual React app here (App.tsx, components
for Import screen / Library / Reader-Player, etc.) starting with Phase 1 of
docs/PRD.md. Delete this file once real source files exist.

Screens needed, per the PRD:
- Import screen (with language auto-detect + override control — PRD 3.1)
- Library (grid/list of books, cover art, progress)
- Reader/Player (word-sync highlighting, speed control, full-screen "Now
  Playing" mode)
